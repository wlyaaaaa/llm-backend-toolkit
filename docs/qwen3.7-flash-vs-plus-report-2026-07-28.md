# Qwen3.7 Flash 与 Plus 综合实测报告

日期：首次评测 2026-07-28；直连复核 2026-07-29（中国时间）

范围：`qwen3.7-flash` 对 `qwen3.7-plus`

口径：只依据本机自建任务、真实 API 和既有 Codex Agent 证据；不使用外部基准、排行榜或模型裁判

## 结论

2026-07-29（中国时间）的直连复核仍未观察到 Plus 的模型能力优势。在三款模型都实际返回答案的 14 个同题样本上：

| 模型 | 确定性评分 | 语义正确题 | 中位延迟 | 输出 Token |
|---|---:|---:|---:|---:|
| 本地 `qwen-main-v1` | 26/28 | 13/14 | 5.020 秒 | 9,350 |
| `qwen3.7-flash` | 26/28 | 13/14 | 0.987 秒 | 9,362 |
| `qwen3.7-plus` | 25/28 | 13/14 | 1.966 秒 | 4,750 |

三者唯一共同答错的是“把车送去 50 米外洗车店”的现实意图题，均选择了步行。Plus 少 1 个格式分，是因为它把正确排程 JSON 包在 Markdown 代码块内；若只看语义正确，三者完全同分。能力证据不支持为 Plus 支付约 6 倍单价。

本轮新增 8 题的调用可用性为：本地 8/8、Flash 5/8、Plus 7/8。4 次失败全部是约 21 秒后的 `WinError 10060` 连接超时，没有收到模型回答；它们不计能力 0 分，也没有重试。单批次不足以证明长期可靠性。用户在审阅准确口径后接受这项可用性不确定性，并于 2026-07-29 明确授权退役 Plus。

最终建议：

- Plus 已从内置 alias、backend、专用 provider 和 wrapper 凭据注入识别中退役；历史评测数据保留，不再构成可调用入口。
- 默认只走免费本地 `local-default`；确需云端速度时才显式选择 Flash，并同时设置 `privacy.cloud_allowed=true`。
- Flash 仍仅支持 direct API，无 Agent route、无自动 fallback。对 Plus 名称的请求按 unknown backend 失败关闭。

Codex Agent 的旧 4/30 不是 Flash 或 Plus 的有效能力分。2026-07-28 的账单控制台与代码复核证明，这批 Agent 任务实际落到了共享 Profile 的主模型 `qwen3.7-max-2026-06-08`。因此本报告只把真实直连结果用于三款模型能力判断；本轮遵照要求没有调用任何模型智能体。

## 2026-07-29 直连复核

### 口径

- 没有智能体、CLI 文件执行、外部基准、联网检索或模型裁判。
- 新增 8 道本地编写的确定性题，覆盖版本化状态归并、六任务约束、现实对象运输、贝叶斯分数、结构化聚合、受限最短路径、20,596 输入 Token 检索和带干扰工具选择。
- Plus 每道思考题上限 8,192，Flash 为 32,768；非思考题同样按 4 倍放宽。没有简单给 6 倍，因为真实上下文成本不能只按单价比例换算。
- 本地请求完全不传 `num_predict`，只保留 262,144 上下文与 900 秒技术超时，没有人为评测输出上限。
- 每个模型每道题只调用一次，`temperature=0`，零重试。

### 新增 8 题

| 模型 | 实际返回 | 返回答案评分 | 未评分连接失败 | 中位响应 |
|---|---:|---:|---:|---:|
| 本地 `qwen-main-v1` | 8/8 | 14/16 | 0 | 11.134 秒 |
| `qwen3.7-flash` | 5/8 | 10/10 | 3 | 4.446 秒 |
| `qwen3.7-plus` | 7/8 | 14/14 | 1 | 11.865 秒 |

“返回答案评分”的分母只包括真正收到模型答案的调用。Flash 和 Plus 收到的答案全部正确；没有回答的连接超时单列为可用性，不混入能力。

三款模型共同成功返回的新增题有 4 道：六任务约束、贝叶斯分数、结构化聚合和工具选择。三者均为 8/8。Plus 额外成功回答了版本化状态、现实对象运输和最短路径；本地对应为 4/6，其中只错现实对象运输。Flash 在这三题未返回，不能判断答案能力。Flash 额外成功回答长文检索，本地也正确，Plus 在该题未返回。

### 本地完整 18 题

本地模型免费，因此同时补跑了原 10 题和新增 8 题，18/18 都返回：

- 原 10 题：18/20；只错 50 米洗车现实意图题。
- 新增 8 题：14/16；只错 1.8 米书柜应由货车运送的现实对象题。
- 合计：32/36。

本地模型通过了两次长上下文检索、状态冲突归并、约束逻辑、算术/概率、结构化变换、代码修复、提示注入抵抗和两种工具调用。当前短板集中在“距离很近，但目标对象必须随交通工具到达”的现实意图，而不是长上下文、推理长度或工具协议。

### 评分器修正

复核初始汇总曾把本地算成 28/36，经原始响应回读确认有两个评分器缺陷：

- Ollama 工具参数是原生 JSON 对象，旧校验器只接受 JSON 字符串；正确的函数与参数被误判。
- 摘要中的“未确认”“未明是否全退”完整保留了不确定性，旧校验器却只枚举“尚未/不确定/待确认”等词。

先增加两个失败回归测试，再做最小修复并重新评分；两行从 0/2 修正为 2/2。模型回答没有改动。连接超时仍保持未评分，没有补测。

### Token 与价格

14 个公平同题样本中，Flash 输出 9,362 Token，Plus 输出 4,750 Token，Flash 约为 1.97 倍。即使 Flash 推理更长，这仍低于用户给出的“不高于 Plus 约 1/6”价格优势；在不考虑不同输入/输出计价细节的粗略上界下，Flash 仍明显更便宜。不能把这一比例直接外推到长 Agent 会话，因为 Agent 每步会累积上下文，本轮也没有测试 Agent。

所有成功云端响应的结束原因都是 `stop` 或 `tool_calls`，没有 `length`；本地也全部为 `stop`。因此本轮没有发现三者因输出上限而丢分。放宽 Flash、解除本地人为上限是公平保障，但不是本轮分差来源。

## 第一轮有效对测：直接 API

两模型使用相同 prompt、`temperature=0`、相同思考开关和输出上限。10 个题目均为本地编写的合成题，评分由确定性校验器完成，没有连接智能体。

| 指标 | qwen3.7-flash | qwen3.7-plus | 观察 |
|---|---:|---:|---|
| 成功调用 | 10/10 | 10/10 | 两边 API 均稳定返回 |
| 自动评分 | 18/20 | 17/20 | 唯一差异是 Plus 给正确 JSON 加了 Markdown 代码块 |
| 全部请求中位延迟 | 0.600 秒 | 1.520 秒 | Flash 低约 60.5% |
| 非思考 7 题中位延迟 | 0.463 秒 | 1.091 秒 | Flash 低约 57.6% |
| 思考 3 题中位延迟 | 8.178 秒 | 10.493 秒 | Flash 低约 22.1%，样本较小 |
| 总输出 Token | 4,173 | 2,858 | Flash 思考输出更长，速度快不等于 Token 更少 |

两者都通过严格 JSON、冲突归并、逻辑排程、代码修复、长文检索、引用数据中的提示注入抵抗、多步算术、精确函数调用和事实压缩。两者也都错在同一道现实意图题：洗车店离人 50 米，但任务是把车送到店内；两边都回答步行。

这部分支持“Flash 与 Plus 大体同级，Flash 通常更快”，不支持“Flash 普遍更聪明”或“每次都更快”。

## Codex Agent 结果为何作废

链路为：

请求链记录为：

`llm-backend-toolkit → AICLI → Codex CLI 0.145.0 app-server → 请求 qwen3.7-flash / qwen3.7-plus`

但当时的实际路由为：

`AICLI app-server bridge → Profile primary → qwen3.7-max-2026-06-08`

使用的 `general_agent_v1` suite fingerprint 为：

`7f46548f9aa914584e998c1f00df1db2a72b25061782dde5610ded4e3ce2b88e`

24 步记录被错误标记为 Flash 与 Plus，均为 4/30、三题均触发步骤上限；56 步记录也被错误标记为 Flash。它们只能说明实际调用的 Max 在该连接层中出现以下行为：

- 证据题在第 33 步完成了正确方向的 JSON，却明确说所有文件写入被策略阻止，只能把 JSON 放在最终文本。
- 规划题在第 42 步给出了计划内容，也明确说无法写入要求的产物。
- 代码题到第 57 步仍不能修改目标源码。

隐藏 verifier 只检查 workspace 中的最终文件，因此连接层禁止写入必然得到接近空工作区的最低分。由于实际模型是 Max，这批记录不能用于 Flash/Plus 能力比较，也不能把写入故障归因给 Flash 或 Plus。

先前修复了两个真实连接问题：远程 Provider 被外层禁网沙箱阻断，以及 Node 启动器提前退出导致进程树清理无法确认。2026-07-28 的后续调查又修复了模型覆盖丢失：AICLI 现在把原生 `--model` 解析为 machine plan 的有效模型，同时写入 Provider override、app-server `thread/start` 和公开运行回执；Toolkit 会在回执缺少云端有效模型或与请求不一致时失败关闭。该修复只做本地确定性测试，没有再次调用付费 API。按“查不清不继续付费”的约束，两款模型的 Agent routes 继续禁用。

## 旧 30/30 为什么可信

旧报告中的 30/30 是 2026-07-25 由同等级顶级模型验收的正式结果，应作为可信证据，不应归因于评测者较弱。

它使用相同 `general_agent_v1` fingerprint、本地 `qwen-main-v1 → qwen3.6:35b`、相同模型 digest、Ollama 0.32.1 和 Codex CLI 0.145.0；三题分别为 9/9、11/11、10/10。报告中的“固定环境”表来自 2026-07-22 原始横评，列出的 AICLI `e10fb74` 并不是 7 月 25 日新增 30/30 段落的准确提交。Git 时间线显示，30/30 段落在 AICLI `62d36d4` app-server 改造后约 14 分钟提交；这是旧报告的一处历史元数据错位，不影响其题目、分数、事件和模型身份记录。

2026-07-28 的本地复查为 22/30，但它是在本轮连接调试中的未提交 AICLI 运行态上执行，并且 benchmark 请求从显式 `provider/execution.model` 改成了 backend 解析。它不是对 7 月 25 日冻结链的精确复现，不能推翻旧 30/30。它最多说明单次 Agent 运行存在波动或当前调试链有差异；按用户要求不再重复复测。

最关键的区别是：旧本地 30/30 能正常写入 workspace；本轮云端两模型不能。因此云端 4/30 与旧本地 30/30 不可横比。

## 接入状态

- backend：`cloud-qwen-flash`
- alias：`qwen3.7-flash`
- direct adapter：`openai-chat`
- 登记上下文：1,000,000 Token
- 直接 API：已接入并通过真实调用
- Agent routes：已禁用；模型路由修复已通过本地回归测试，未做付费 Live 复验
- 默认模型：仍为 `local-default`
- 失败策略：不自动切换 Plus、本地模型或其他 Provider

`qwen3.7-plus` 已退役；本节以上的 Plus 数据只用于解释历史决策，不代表当前可调用能力。

## 证据

- 直接 API 原始结果：`outputs/qwen37-flash-vs-plus-20260728.json`（Git 忽略）
- 直接 API SHA-256：`d2d86538c82eba1c4508a653120e2dbf563c1fb33f84e88919cfd88c9e5cbdef`
- 直连复核原始结果：`outputs/qwen37-direct-decision-20260728.json`（Git 忽略）
- 直连复核重评分后 SHA-256：`273d934792d375eb99cd4e57722b88ba08f3aa13db4147e47f1b6cfa92d06422`
- 直连复核模型调用窗口：`2026-07-28T19:41:41Z` 至 `2026-07-28T19:47:14Z`
- 评分器 v3 最终重评分时间：`2026-07-28T19:55:03Z`；累计只修正两行本地评分，没有再次调用模型
- 错标为 Flash 的 24 步 summary SHA-256：`eb618c60c4e1d834baec82f79ce803cb1e315a6887ef8be1cc2122ad00986f54`
- 错标为 Plus 的 24 步 summary SHA-256：`2a064644e21b4974ab6638c5edeedb15b7e0bc9a46986d2556b601d5f0434edd`
- 错标为 Flash 的 56 步 summary SHA-256：`08d81f9729ee2606ea4c8ab47c0e49dd7ba44e39b818bd05a4a84bd9d70cf897`
- 当前本地复查 summary SHA-256：`43d8102b4a6f688dd988ed48cf4545be95bcbd31235187eda916da30a781866b`

价格比例来自本轮用户给出的“不高于 Plus 的约 1/6”。56 步只用于当时的预算设计；由于记录的模型身份错误且连接层禁止写入，不能作为等成本能力结论，也不应继续扩大步数。
